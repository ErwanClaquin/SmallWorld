class research:
    def searchUniteOnCase(self, army, listeCase):
        listeUnite = []
        for element in army:
            for case in listeCase:
                if element in case:
                    print("pas fait")
